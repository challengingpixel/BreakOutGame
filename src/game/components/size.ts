// Component: Size
// Stores the width and height of a rectangular entity.
// Used by the paddle and bricks for collision detection and rendering.

export interface SizeComponent {
  width: number;
  height: number;
}
