// Component: Velocity
//
// dx = horizontal speed (positive = right, negative = left)
// dy = vertical speed   (positive = down,  negative = up)
export function createVelocity(dx, dy) {
    return { dx, dy };
}
